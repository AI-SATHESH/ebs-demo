name: Deploy to Elastic Beanstalk

on:
  push:
    branches:
      - main  # Change to your main branch if different

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Package application
        run: |
          zip -r my-fastapi-app.zip .  # Adjust the command as needed

      - name: Deploy to Elastic Beanstalk
        uses: einaregilsson/beanstalk-deploy@v20
        with:
          application_name: github-actionsdemo
          environment_name: Github-actionsdemo-env
          version_label: ${{ github.sha }}
          region: ap-south-1
          bucket_name: githubactionsdemo123
          bucket_key: my-fastapi-app.zip
          access_key_id: ${{ secrets.AKIAWPPO6VIHL3ULIGZN }}
          secret_access_key: ${{ secrets.MjmlW+4eqXUPARisvF5JxGCQ5UtBTx/CY9lsn19p }}

